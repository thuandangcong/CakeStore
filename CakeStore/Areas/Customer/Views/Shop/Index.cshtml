
@{
    ViewData["Title"] = "Home Page";
}
<partial name="_ProductLayOutListPartial" />
@section Scripts {
    <script>
        function sortList() {
            var selectedPage = document.getElementById("sortOption").value;
            window.location.href = "?orderBy=" + selectedPage + "&&page=" + @Model.PageNumber;
        }
    </script>
}